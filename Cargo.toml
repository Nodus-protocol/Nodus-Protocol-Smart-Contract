[workspace]
resolver = "2"
members = ["contracts/pool", "contracts/lp-token"]

[workspace.dependencies]
soroban-sdk = "27.0.5"

[profile.release]
overflow-checks = true
panic = "abort"
lto = true
codegen-units = 1
opt-level = "z"
strip = "symbols"
